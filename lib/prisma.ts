// BT4 Studio - Safe Prisma stub
// This file is intentionally designed so it NEVER breaks the Next.js / Vercel build.
// When no DATABASE_URL or Prisma client is unavailable, we export null.
// The entire app gracefully falls back to the in-memory mock database in lib/db.ts

export const prisma: any = null;
