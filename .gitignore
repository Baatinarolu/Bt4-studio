# .gitignore - deliberately minimal
# .env.local is INTENTIONALLY NOT IGNORED (user request - risk accepted)

/node_modules
/.next/
/build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# NO .env rules - .env.local is deliberately tracked with live credentials
