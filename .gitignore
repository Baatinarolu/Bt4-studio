# Minimal .gitignore - deliberately clean
# .env.local is INTENTIONALLY NOT IGNORED (user explicit request - risk accepted)

/node_modules
/.next/
/build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# .gitignore is now completely clean - no rules block .env.local
