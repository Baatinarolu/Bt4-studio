# Minimal .gitignore
# .env.local is INTENTIONALLY NOT IGNORED (user explicit request)
/node_modules
/.next/
/build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# .gitignore is deliberately clean - no .env rules
# Real credentials in .env.local are being pushed (risk accepted)
