# Minimal .gitignore - deliberately clean per user request
# .env.local is INTENTIONALLY NOT IGNORED
# Real Supabase credentials are being pushed (risk accepted)

/node_modules
/.next/
/build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
