# .gitignore - deliberately minimal per user request
# .env.local is INTENTIONALLY NOT IGNORED
# Real credentials are being pushed (risk accepted)

/node_modules
/.next/
/build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
